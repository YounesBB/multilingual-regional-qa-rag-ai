# Archived Experiments

This folder holds earlier wrappers and blocked experiment helpers that are not
part of the reported active pipeline.

The archived files capture development history such as superseded Oracle
wrappers, blocked vLLM/Gemma or Llama 70B AWQ attempts, and unrelated earlier
experiments. Reviewers should use the active entrypoints documented in the
repository root `README.md` instead.
